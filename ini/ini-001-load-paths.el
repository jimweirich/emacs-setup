;;; ==================================================================
;;; Author:  Jim Weirich
;;; File:    ini-001-load-paths.el
;;; Purpose: Define the default load paths used by emacs.
;;; ==================================================================

(add-to-load-path (concat elisp-directory "/pkgs"))
(add-to-load-path (concat elisp-directory "/local-pkgs"))
